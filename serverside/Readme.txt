To run this project 

1) Open the terminal in serverside and run "npm install", after the package installed then use "nodemon run server" command


2) Open the terminal in clientside and run "npm install", after the package installed then run "npm start" ...


There you go..!!!
